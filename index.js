// 1. Function Expression for divide
const divide = function() {
    return 2000 / 100;
  };
  
  // 2. Arrow Function for square
  const square = (num) => num * num;
  
  // 3. Arrow Function for add
  const add = (a, b) => a + b;
  